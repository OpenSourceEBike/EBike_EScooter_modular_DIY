# Lights-only config (delegates to the selected model in config.py)
from common import config_runtime as _cfg

# ===================================================================
# MAC ADDRESS (lights board)
# ===================================================================
mac_address_lights = _cfg.mac_address_lights
